openapi: 3.1.0
paths:
  /v1/user/update:
    post:
      summary: 'Form post'
      requestBody:
        content:
          multipart/form-data:
            schema:
              properties:
                name: {}
                avatar:
                  description: 'file to upload'
                  type: string
                  format: binary
              type: object
      responses:
        '200':
          description: Success
